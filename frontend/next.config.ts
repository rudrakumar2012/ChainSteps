import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack config - Next.js 16 defaults to Turbopack
  // No way to disable - use webpack via Next.js 15 or environment trick
};

export default nextConfig;
